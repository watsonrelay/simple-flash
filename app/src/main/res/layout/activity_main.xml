<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context="com.liverelay.app.flashlight.MainActivity">

    <!-- dummy surface view to enable camera -->
    <SurfaceView
        android:id="@+id/dummy_surface_view"
        android:layout_width="1dp"
        android:layout_height="1dp" />

    <!-- The primary full-screen view -->
    <ImageView
        android:id="@+id/power_button_image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:src="@drawable/power_symbol" />

</FrameLayout>
